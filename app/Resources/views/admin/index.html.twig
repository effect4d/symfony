{% extends 'base.html.twig' %}

{% block body_id 'admin' %}

{% block main %}
    <div class="row">
        <div class="col-sm-9">
            <table class="table table-striped table-middle-aligned">
                <thead>
                    <tr>
                        <th scope="col">Username</th>
                        <th scope="col">Email</th>
                        <th scope="col">Role</th>
                        <th scope="col">Is Active</th>
                        <th scope="col" class="text-right">Action</th>
                    </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>{{ user.email }}</td>
                        <td>{{ user.roles|join(', ') }}</td>
                        <td>
                            {% if user.IsActive %}
                                On
                            {% else %}
                                Off
                            {% endif %}
                        </td>
                        <td class="text-right">
                            <div class="item-actions">
                                <a href="{{ path('user_edit', { id: user.id }) }}" class="btn btn-sm btn-primary">
                                    <i class="fa fa-edit" aria-hidden="true"></i> edit!
                                </a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="col-sm-3">
            <a href="{{ path('create') }}" class="btn btn-lg btn-block btn-success">
                <i class="fa fa-plus" aria-hidden="true"></i> Create user
            </a>
        </div>
    </div>
{% endblock %}
